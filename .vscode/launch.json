{
	"launch": {
		"version": "0.2.0",
		"configurations": [
			{
				"name": "지금 파일 디버깅",
				"type": "node",
				"request": "launch",
				"program": "${file}",
				"cwd": "${fileDirName}"
			}
		]
	}
}
